mixin post(msg , subject, pic, postedBy)
    .post-body
        h6
        img(pic)
        h2.text-body=subject 
        p.text-body=msg 
        p.text-body="posted by " + postedBy
        

    //- h2= subject 
    //- img(pic)
    //- p.message=msg 
    //- p.postedBy=postedBy 